 <button id="ajax-new-order">New Order</button>
  <div id="new-order-form-div">[form..]</div>






<%= form_for(@order) do |f| %>    
    <%= f.collection_select :meal_id, Meal.all, :id, :name %>
    <%= f.select :quantity, options_for_select(1..20), { prompt: "quantity" } %>
    <%= f.submit  %>
  </div>
<% end %>




